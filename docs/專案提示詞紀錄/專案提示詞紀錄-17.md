## Copilot 提問 管理頁抽象化
能不能抽象 一般化，並加入 搜尋欄 上傳功能 可選
```
// src/pages/DataTablesPage.tsx
import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import {
  Box,
  Typography,
  TextField,
  Button,
  Grid,
  ToggleButtonGroup,
  ToggleButton,
} from "@mui/material";
import FormatListBulletedIcon from "@mui/icons-material/FormatListBulleted";
import AddIcon from "@mui/icons-material/Add";
import UploadIcon from "@mui/icons-material/Upload";
import GridViewIcon from "@mui/icons-material/GridView";
import { PageWrapper } from "../components/layout/PageWrapper";
import { DataTableList } from "../components/DataTablesPage/DataTableList";
import { UploadDataTableDialog } from "../components/DataTablesPage/UploadDataTableDialog";
import { useUploadStore } from "../stores/uploadStore";
import type { DataTableInfo } from "shared/types/dataTable";
import type { CreateTableNavigateState, PageConfig } from "../types";
import { UploadStatusPanel } from "../components/DataTablesPage/UploadStatusPanel"; // 新增元件

export const DataTablesPage = () => {
  const [searchText, setSearchText] = useState("");
  const [uploadDialogOpen, setUploadDialogOpen] = useState(false);
  const [viewMode, setViewMode] = useState<"card" | "list">("card");
  const [tableInfos, setTableInfos] = useState<DataTableInfo[]>([]);
  const navigate = useNavigate();

  // 從 Zustand Store 獲取上傳狀態
  const uploads = useUploadStore((state) => state.uploads);
  const resetUploads = useUploadStore((state) => state.reset);

  // 監聽 uploads 狀態的變化，並在有成功上傳時刷新列表
  useEffect(() => {
    console.log("Uploads state changed:", uploads);
    if (uploads.some((u) => u.status === "success")) {
      console.log("Detected successful upload, refreshing table infos...");
      refreshTableInfos();
    }
  }, [uploads]);

  useEffect(() => {
    refreshTableInfos();
    // 頁面初次載入時清空上傳狀態，避免上次的紀錄影響本次
    return () => {
      resetUploads();
    };
  }, [resetUploads]);

  const refreshTableInfos = () => {
    window.api.getAllTableInfos().then((tables) => {
      setTableInfos(tables);
    });
  };

  // 根據搜尋關鍵字過濾資料
  const filteredDataTables = tableInfos.filter((table) =>
    table.name.toLowerCase().includes(searchText.toLowerCase())
  );

  const handleViewModeChange = (
    _event: React.MouseEvent<HTMLElement>,
    newViewMode: "card" | "list"
  ) => {
    if (newViewMode !== null) {
      setViewMode(newViewMode);
    }
  };

  const handleNewTableClick = () => {
    const state: CreateTableNavigateState = { editorMode: "create" };
    navigate("/data-tables/edit", { state });
  };

  // 頁面配置
  const pageConfig: Omit<PageConfig, "tocItems"> = {
    breadcrumbItems: [{ label: "資料表格管理", path: "/data-tables" }],
    content: (
      <Box sx={{ p: 3 }}>
        <Grid container alignItems="center" spacing={2} sx={{ mb: 3 }}>
          <Grid
            size={{ xs: 12, sm: 6 }}
            container
            alignItems="center"
            spacing={2}
          >
            {/* 標題與模式切換按鈕 */}
            <Grid>
              <Typography variant="h4" sx={{ fontWeight: "bold" }}>
                資料表格管理
              </Typography>
            </Grid>
            <Grid>
              <ToggleButtonGroup
                value={viewMode}
                exclusive
                onChange={handleViewModeChange}
                aria-label="view mode"
                size="small"
              >
                <ToggleButton value="card" aria-label="card view">
                  <GridViewIcon />
                </ToggleButton>
                <ToggleButton value="list" aria-label="list view">
                  <FormatListBulletedIcon />
                </ToggleButton>
              </ToggleButtonGroup>
            </Grid>
          </Grid>
          <Grid
            size={{ xs: 12, sm: 6 }}
            container
            justifyContent="flex-end"
            spacing={1}
          >
            {/* 搜尋框 */}
            <Grid>
              <TextField
                label="搜尋表格"
                variant="outlined"
                size="small"
                value={searchText}
                onChange={(e) => setSearchText(e.target.value)}
              />
            </Grid>
            {/* 新增表格按鈕 */}
            <Grid>
              <Button
                variant="contained"
                startIcon={<AddIcon />}
                onClick={() => handleNewTableClick()}
              >
                新增資料表格
              </Button>
            </Grid>
            {/* 上傳按鈕 */}
            <Grid>
              <Button
                variant="contained"
                startIcon={<UploadIcon />}
                onClick={() => setUploadDialogOpen(true)}
              >
                上傳資料表格
              </Button>
            </Grid>
          </Grid>
        </Grid>

        {/* 資料表格列表 */}
        <DataTableList
          dataTables={filteredDataTables}
          viewMode={viewMode}
          refresh={refreshTableInfos}
        />

        {/* 上傳資料表格對話框 */}
        <UploadDataTableDialog
          open={uploadDialogOpen}
          onClose={() => setUploadDialogOpen(false)}
        />
      </Box>
    ),
    rightPanelContent: <UploadStatusPanel />, // 這裡使用新元件
    rightPanelTitle: "上傳狀態",
  };

  return <PageWrapper {...pageConfig} />;
};
```

## Copilot 回答 管理頁抽象化
可以的！你目前的 `DataTablesPage.tsx` 結構非常清晰，我會幫你抽象成一個通用的頁面元件，命名為：

> **`GenericListPage.tsx`**

這個元件會支援：
- 搜尋欄（可選）
- 新增按鈕（可選）
- 上傳按鈕（可選）
- 檢視模式切換（卡片 / 列表）
- 列表內容由 `GenericListView` 呈現

---

### ✅ 1. 建立 `GenericListPage.tsx`

```tsx
// src/components/common/GenericListPage.tsx
import {
  Box,
  Grid,
  Typography,
  TextField,
  Button,
  ToggleButtonGroup,
  ToggleButton,
} from "@mui/material";
import GridViewIcon from "@mui/icons-material/GridView";
import FormatListBulletedIcon from "@mui/icons-material/FormatListBulleted";
import AddIcon from "@mui/icons-material/Add";
import UploadIcon from "@mui/icons-material/Upload";
import { useState } from "react";

export interface GenericListPageProps<T> {
  title: string;
  items: T[];
  viewMode?: "card" | "list";
  searchable?: boolean;
  uploadable?: boolean;
  creatable?: boolean;
  searchPlaceholder?: string;
  onSearch?: (keyword: string) => void;
  onCreate?: () => void;
  onUpload?: () => void;
  renderList: (filteredItems: T[], viewMode: "card" | "list") => React.ReactNode;
}

export function GenericListPage<T>({
  title,
  items,
  viewMode = "card",
  searchable = true,
  uploadable = false,
  creatable = false,
  searchPlaceholder = "搜尋",
  onSearch,
  onCreate,
  onUpload,
  renderList,
}: GenericListPageProps<T>) {
  const [searchText, setSearchText] = useState("");
  const [currentViewMode, setCurrentViewMode] = useState(viewMode);

  const handleSearchChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const keyword = e.target.value;
    setSearchText(keyword);
    onSearch?.(keyword);
  };

  const handleViewModeChange = (_: any, mode: "card" | "list") => {
    if (mode) setCurrentViewMode(mode);
  };

  return (
    <Box sx={{ p: 3 }}>
      <Grid container alignItems="center" spacing={2} sx={{ mb: 3 }}>
        <Grid item xs={12} sm={6}>
          <Typography variant="h4" fontWeight="bold">{title}</Typography>
          <ToggleButtonGroup
            value={currentViewMode}
            exclusive
            onChange={handleViewModeChange}
            size="small"
            sx={{ mt: 1 }}
          >
            <ToggleButton value="card"><GridViewIcon /></ToggleButton>
            <ToggleButton value="list"><FormatListBulletedIcon /></ToggleButton>
          </ToggleButtonGroup>
        </Grid>

        <Grid item xs={12} sm={6}>
          <Grid container justifyContent="flex-end" spacing={1}>
            {searchable && (
              <Grid item>
                <TextField
                  label={searchPlaceholder}
                  variant="outlined"
                  size="small"
                  value={searchText}
                  onChange={handleSearchChange}
                />
              </Grid>
            )}
            {creatable && (
              <Grid item>
                <Button variant="contained" startIcon={<AddIcon />} onClick={onCreate}>
                  新增
                </Button>
              </Grid>
            )}
            {uploadable && (
              <Grid item>
                <Button variant="contained" startIcon={<UploadIcon />} onClick={onUpload}>
                  上傳
                </Button>
              </Grid>
            )}
          </Grid>
        </Grid>
      </Grid>

      {/* 列表內容 */}
      {renderList(items, currentViewMode)}
    </Box>
  );
}
```

---

### ✅ 2. 在 `DataTablesPage.tsx` 中使用

你可以這樣改寫：

```tsx
import { GenericListPage } from "../components/common/GenericListPage";
import { DataTableList } from "../components/DataTablesPage/DataTableList";

export const DataTablesPage = () => {
  const [searchText, setSearchText] = useState("");
  const [uploadDialogOpen, setUploadDialogOpen] = useState(false);
  const [tableInfos, setTableInfos] = useState<DataTableInfo[]>([]);
  const navigate = useNavigate();

  const uploads = useUploadStore((state) => state.uploads);
  const resetUploads = useUploadStore((state) => state.reset);

  useEffect(() => {
    if (uploads.some((u) => u.status === "success")) {
      refreshTableInfos();
    }
  }, [uploads]);

  useEffect(() => {
    refreshTableInfos();
    return () => resetUploads();
  }, [resetUploads]);

  const refreshTableInfos = () => {
    window.api.getAllTableInfos().then(setTableInfos);
  };

  const filteredTables = tableInfos.filter((t) =>
    t.name.toLowerCase().includes(searchText.toLowerCase())
  );

  return (
    <PageWrapper
      breadcrumbItems={[{ label: "資料表格管理", path: "/data-tables" }]}
      content={
        <GenericListPage
          title="資料表格管理"
          items={filteredTables}
          searchable
          creatable
          uploadable
          searchPlaceholder="搜尋表格"
          onSearch={setSearchText}
          onCreate={() => navigate("/data-tables/edit", { state: { editorMode: "create" } })}
          onUpload={() => setUploadDialogOpen(true)}
          renderList={(items, viewMode) => (
            <DataTableList dataTables={items} viewMode={viewMode} refresh={refreshTableInfos} />
          )}
        />
      }
      rightPanelContent={<UploadStatusPanel />}
      rightPanelTitle="上傳狀態"
    />
  );
};
```

---

### ✅ 優點總結

- ✅ 可重複使用於 Dashboard、User、Chart 等模組
- ✅ 搜尋、新增、上傳功能可選擇性開啟
- ✅ 列表內容由外部元件控制，保留彈性
- ✅ UI 統一、邏輯清晰

---

是否需要我幫你也套用到 `DashboardPage` 或 `UserPage`？或者你想先測試這個版本？

### 推薦兩問
- 幫我把這套用在 DashboardPage 範例
- UserPage 要怎麼整合這個 GenericListPage

## Gemini 提問 儀表板功能實作-瀏覽/編輯/上傳/刪除
> 另開新一頁

你是一名專業的全端桌面軟體程式設計師，有多年使用 Electron + better-sqlite3 + Vite + React 的豐富經驗，現在面對以下專案和問題

### 🧩 專案背景：
- 使用技術：
  - 前端 Vite + React + MUI + Zustand + TypeScript
  - 後端 Electron + better-sqlite3
- 專案類型：資料視覺化儀表板
- 目前架構：
```front end
  src/
  ├── assets/data.json, snake.jpg
  ├── components/common/ConfirmCancelButtons.tsx, DataTable.tsx, DeleteWarningDialog.tsx, EditableCell.tsx, EditableTitle.tsx, ExpandableStatusBar.tsx, GenericItemActions.tsx, GenericListPage.tsx, GenericListView.tsx, Logo.tsx, PageHeader.tsx, SimpleTable.tsx
  ├── components/DashboardsPage/DashboardList.tsx
  ├── components/DataTablesPage/DataTableList.tsx, StandardFormatDialog.tsx, UploadDataTableDialog.tsx, uploadHelpers.tsx, UploadStatusBar.tsx, UploadStatusPanel.tsx
  ├── components/layout/Breadcrumb.tsx, layout.css, Layout.tsx, PageWrapper.tsx, RightPanel.tsx, Sidebar.tsx, TocList.tsx, TocListItem.tsx, TopNav.tsx
  ├── context/LayoutContext.tsx, LayoutProvider.tsx, useLayoutContext.tsx
  ├── hooks/useDebounce.tsx, useFileParser.tsx, useTableDataInitializer.tsx, useTableEditor.tsx, useTableGetter.tsx
  ├── pages/DashboardsPage.tsx, DataTableEditorPage.tsx, DataTablesPage.tsx, HomePage.tsx
  ├── stores/layoutStore.ts, uploadStore.ts
  ├── theme/index.ts
  ├── App.tsx
  ├── constants.ts
  ├── main.tsx
  ├── mui.d.ts
  ├── preload.d.ts
  ├── types.tsx
  ├── utils.tsx
  └── vite-env.d.ts
```
```backend
  electron/
  ├── ipc/APIModule.cts, ChartAPIHandlers.cts, DashboardAPIHandlers.cts, DataTableAPIHandlers.cts
  ├── models/ChartManager.cts, DatabaseManager.cts, DataTableManager.cts, FileManager.cts
  ├── services/ChartService.cts, DataTableService.cts
  ├── windows/mainWindow.cts
  ├── constants.cts
  ├── main.cts
  ├── preload.cts
  ├── tsconfig.json
  ├── types.cts
  └── utils.cts
```

### 🧱 已完成項目：
- ipc: 綁定 DataTable 資料表相關 API
- 使用 PageWrapper 完成排版相關的包裝
- DataTable 相關增刪修(前後端)皆已完成

### 🔧 目前狀態：
- 前端: 完成儀表板管理頁面 DashboardsPage 初步架構: 卡片/列表切換顯示、搜尋框
- 前端: 儀表板設定檔上傳對話框、儀表板瀏覽互動、編輯、刪除尚未實作
- 後端: 儀表板儲存、更新、刪除尚未實作

### ❓接下來想處理的問題：
在儀表板管理頁面 DashboardsPage 中，以卡片或列表形式羅列儀表板資訊，包含名稱、更新日期、圖表數量等。右上角提供新增和上傳兩個按鈕，除此之外，每一個儀表板名稱是可以點擊的連結，且提供更複雜操作的選單按鈕(包含更新、匯出、刪除等操作)。

- 點擊儀表板名稱，可連結對應儀表板的瀏覽頁面，此瀏覽頁面用於展示統計圖表，並可與使用者互動。右上角有一個編輯按鈕(鉛筆圖示)，可切換到編輯模式。編輯模式包含新增圖表、拖拉圖表大小、移動圖表位置、編輯圖表內容設定、刪除圖表等。統計圖表的實現暫時擱置，先用一個空白區塊替代。

- 點擊新增按鈕，跳到儀表板瀏覽頁面，並直接進入編輯模式
- 點擊上傳按鈕，出現上傳面板對話框，供使用者上傳儀表板設定檔
- 點擊刪除按鈕，出現刪除警告對話框，再次確認

附上 DataTable 資料表相關檔案，希望能統一風格

> 附件 `DashboardsPage.tsx`, `DataTablesPage.tsx`, `DashboardList.tsx`, `DataTableList.tsx`, `UploadDataTableDialog.tsx`

## Gemini 回答 儀表板功能實作-瀏覽/編輯/上傳/刪除
作為一名專業的全端桌面軟體程式設計師，我理解您希望將 **儀表板管理頁面 (`DashboardsPage`)** 完善，使其具備與 **資料表格管理頁面 (`DataTablesPage`)** 相似且一致的功能和使用者體驗，特別是關於**操作介面**、**導航**和**列表互動**。

根據您的需求和現有程式碼架構，我將對 `DashboardsPage.tsx`、`DashboardList.tsx` 進行修改，並新增 `UploadDashboardDialog.tsx` 以實作上傳功能，確保整體風格和邏輯的統一。

由於儀表板的**新增/編輯**和**瀏覽**邏輯需要一個專門的頁面來實現，我將新增一個架構文件來定義 **儀表板編輯頁面 (`DashboardEditorPage.tsx`)** 和 **儀表板瀏覽頁面 (`DashboardViewPage.tsx`)** 的基本架構，並確保點擊邏輯正確導航。

-----

### 1\. 完善儀表板清單頁面 (`DashboardsPage.tsx`)

為了實現**新增**和**上傳**功能，我們需要更新 `DashboardsPage.tsx`，使其行為與 `DataTablesPage.tsx` 一致。

#### `src/pages/DashboardsPage.tsx`

修改點：

1.  匯入 `useNavigate`。
2.  新增 `uploadDialogOpen` 狀態來控制上傳對話框。
3.  實作 `handleNewDashboardClick` 函式，用於點擊「新增」按鈕時導航到編輯頁面。
4.  更新 `GenericListPage` 的 `creatable` 和 `uploadable` 屬性，並實作對應的 `onCreate` 和 `onUpload` 處理函式。
5.  在 `content` 中引入即將新增的 `UploadDashboardDialog` 元件。

<!-- end list -->

```tsx
// src/pages/DashboardsPage.tsx
import type { PageConfig, CreateDashboardNavigateState } from "src/types"; // 假設新增 CreateDashboardNavigateState
import { PageWrapper } from "../components/layout/PageWrapper";
import { GenericListPage } from "../components/common/GenericListPage";
import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom"; // 1. 匯入 useNavigate
import type { DashboardInfo } from "shared/types/dashboard";
import { DashboardList } from "../components/DashboardsPage/DashboardList";
import { UploadDashboardDialog } from "../components/DashboardsPage/UploadDashboardDialog"; // 引入上傳對話框

export const DashboardsPage = () => {
  const [searchText, setSearchText] = useState("");
  const [dashboardInfos, setDashboardInfos] = useState<DashboardInfo[]>([]); // 修正為 setDashboardInfos
  const [uploadDialogOpen, setUploadDialogOpen] = useState(false); // 2. 新增上傳對話框狀態
  const navigate = useNavigate(); // 1. 初始化

  const refreshDashboardInfos = () => {
    // 實作：呼叫後端 API 取得儀表板資訊
    // window.api.getAllDashboardInfos().then(setDashboardInfos);
    // 暫時使用 mock data
    setDashboardInfos([
      {
        id: "d1",
        name: "儀表板 A",
        chartCount: 3,
        created_at: "2023-10-01 10:00:00",
        updated_at: "2023-10-05 11:30:00",
      },
      {
        id: "d2",
        name: "儀表板 B",
        chartCount: 5,
        created_at: "2023-09-15 09:00:00",
        updated_at: "2023-10-06 14:00:00",
      },
    ]);
  };

  useEffect(() => {
    refreshDashboardInfos();
  }, []);

  // 3. 處理新增儀表板點擊
  const handleNewDashboardClick = () => {
    const state: CreateDashboardNavigateState = { editorMode: "create" }; // 假設定義了該 type
    // 導航到儀表板編輯頁面，並帶入 state 讓其進入編輯模式
    navigate("/dashboards/edit", { state });
  };

  // 根據搜尋關鍵字過濾資料
  const filteredDashboards = dashboardInfos.filter((t) =>
    t.name.toLowerCase().includes(searchText.toLowerCase())
  );

  // 頁面配置
  const pageConfig: Omit<PageConfig, "tocItems"> = {
    breadcrumbItems: [{ label: "儀表板管理", path: "/dashboards" }],
    content: (
      <>
        <GenericListPage
          title="儀表板管理"
          items={filteredDashboards}
          searchable
          creatable // 4. 開啟新增按鈕
          uploadable // 4. 開啟上傳按鈕
          searchPlaceholder="搜尋儀表板"
          onSearch={setSearchText}
          onCreate={handleNewDashboardClick} // 4. 實作新增處理
          onUpload={() => setUploadDialogOpen(true)} // 4. 實作上傳處理
          renderList={(items, viewMode) => (
            <DashboardList
              dashboards={items}
              viewMode={viewMode}
              refresh={refreshDashboardInfos}
            />
          )}
        />
        {/* 5. 上傳儀表板對話框 */}
        <UploadDashboardDialog
          open={uploadDialogOpen}
          onClose={() => setUploadDialogOpen(false)}
        />
      </>
    ),
    rightPanelContent: null,
    rightPanelTitle: "上傳狀態",
  };

  return <PageWrapper {...pageConfig} />;
};
```

-----

### 2\. 完善儀表板清單元件 (`DashboardList.tsx`)

為了統一操作體驗，我們需要確保儀表板列表中的點擊行為和操作選單邏輯與 `DataTableList.tsx` 保持一致，並使用 `GenericItemActions` 實作刪除確認對話框。

#### `src/components/DashboardsPage/DashboardList.tsx`

修改點：

1.  修改 `handleAction` 邏輯，準備呼叫後端刪除 API，並確保刪除時會出現確認對話框。

<!-- end list -->

```tsx
// src/components/DashboardsPage/DashboardList.tsx
import { useNavigate } from "react-router-dom";
import { Box } from "@mui/material";
import { GenericListView, type GenericItem } from "../common/GenericListView";
import { GenericItemActions } from "../common/GenericItemActions";
import type { DashboardInfo } from "shared/types/dashboard";

interface Props {
  dashboards: DashboardInfo[];
  viewMode: "card" | "list";
  refresh: () => void;
}

export const DashboardList = ({ dashboards, viewMode, refresh }: Props) => {
  const navigate = useNavigate();

  // 點擊項目名稱時導向**瀏覽頁面**
  const handleClickItem = (id: DashboardInfo["id"]) => {
    console.log(`點擊了儀表板 ${id}，導向瀏覽頁`);
    navigate(`/dashboards/view/${id}`);
  };

  // 執行操作（更新、刪除等）
  const handleAction = (action: string, id: DashboardInfo["id"]) => {
    console.log(`對儀表板 ${id} 執行操作: ${action}`);
    if (action === "delete") {
      // 實作：呼叫後端 API 刪除儀表板
      // window.api.deleteDashboard(id); 
      refresh(); // 刷新列表
    }
    // 其他操作邏輯可擴充
  };

  // 將 DashboardInfo 轉換成 GenericItem
  const genericItems: GenericItem<DashboardInfo["id"]>[] = dashboards.map(
    (d) => ({
      id: d.id,
      title: d.name,
      // 將 subtitle 內容簡化並放入 metadata，統一使用 metadata 展示輔助資訊
      subtitle: `更新時間：${d.updated_at}`,
      metadata: {
        圖表數量: d.chartCount.toString(),
        更新時間: d.updated_at, // 可以選擇放在這裡
      },
    })
  );

  return (
    <Box>
      <GenericListView
        items={genericItems}
        viewMode={viewMode}
        onClickItem={handleClickItem} // 點擊導向瀏覽頁
        renderActions={(id) => (
          <GenericItemActions
            itemId={id}
            actions={[
              { key: "update", label: "更新" },
              { key: "export", label: "匯出" },
              { key: "delete", label: "刪除" },
            ]}
            onAction={handleAction}
            confirmActions={["delete"]} // 刪除操作需要確認
            confirmMessages={{
              delete: {
                title: "刪除儀表板",
                content: "確定要刪除這個儀表板嗎？此操作無法復原。",
              },
            }}
          />
        )}
      />
    </Box>
  );
};
```

-----

### 3\. 新增儀表板上傳對話框 (`UploadDashboardDialog.tsx`)

為了處理上傳儀表板設定檔的需求，我們新增 `UploadDashboardDialog.tsx`。由於儀表板設定檔通常是單一 JSON 檔案，這裡的邏輯可以比 `UploadDataTableDialog.tsx` 稍微簡化，專注於單檔案上傳，並直接導航到編輯頁面。

#### `src/components/DashboardsPage/UploadDashboardDialog.tsx` (新檔案)

```tsx
// src/components/DashboardsPage/UploadDashboardDialog.tsx
import { useState } from "react";
import {
  Dialog,
  DialogTitle,
  DialogContent,
  DialogActions,
  Button,
  Typography,
  Box,
  IconButton,
} from "@mui/material";
import CloudUploadIcon from "@mui/icons-material/CloudUpload";
import CloseIcon from "@mui/icons-material/Close";
import { useNavigate } from "react-router-dom";
import type { UploadDashboardNavigateState } from "../../types"; // 假設新增此 type

interface Props {
  open: boolean;
  onClose: () => void;
}

export const UploadDashboardDialog = ({ open, onClose }: Props) => {
  const [file, setFile] = useState<File | null>(null);
  const [isDragOver, setIsDragOver] = useState(false);
  const navigate = useNavigate();

  // --- 事件處理器 ---
  const handleCancel = () => {
    setFile(null); // 清空狀態
    onClose();
  };

  const handleFileChange = (event: React.ChangeEvent<HTMLInputElement>) => {
    if (event.target.files && event.target.files.length > 0) {
      // 只處理第一個檔案
      const selectedFile = event.target.files[0];
      if (selectedFile.type === "application/json") {
        setFile(selectedFile);
      }
    }
  };

  const handleRemoveFile = () => {
    setFile(null);
  };

  // 拖曳處理器（與 UploadDataTableDialog 邏輯相同）
  const handleDragOver = (event: React.DragEvent<HTMLDivElement>) => {
    event.preventDefault();
    setIsDragOver(true);
  };

  const handleDragLeave = (event: React.DragEvent<HTMLDivElement>) => {
    event.preventDefault();
    setIsDragOver(false);
  };

  const handleDrop = (event: React.DragEvent<HTMLDivElement>) => {
    event.preventDefault();
    setIsDragOver(false);
    if (event.dataTransfer?.files && event.dataTransfer.files.length > 0) {
      const droppedFile = event.dataTransfer.files[0];
      if (droppedFile.type === "application/json") {
        setFile(droppedFile);
      }
    }
  };

  // 處理確認上傳 (導航到編輯頁面)
  const handleUpload = () => {
    if (!file) return;

    console.log("上傳儀表板設定檔，導航至編輯頁面...");
    onClose();
    const state: UploadDashboardNavigateState = {
      editorMode: "upload",
      file: file,
    };
    // 導航到儀表板編輯頁面，並帶入上傳模式 state
    navigate("/dashboards/edit", { state });
  };

  return (
    <Dialog open={open} onClose={handleCancel} maxWidth="sm" fullWidth>
      <DialogTitle>上傳儀表板設定檔</DialogTitle>
      <IconButton
        aria-label="close"
        onClick={handleCancel}
        sx={(theme) => ({
          position: "absolute",
          right: 8,
          top: 8,
          color: theme.palette.grey[500],
        })}
      >
        <CloseIcon />
      </IconButton>
      <DialogContent dividers>
        <Typography variant="subtitle1" gutterBottom>
          請選擇儀表板 JSON 設定檔
        </Typography>
        <Box
          onDragOver={handleDragOver}
          onDragLeave={handleDragLeave}
          onDrop={handleDrop}
          sx={{
            border: "2px dashed #ccc",
            borderColor: isDragOver ? "primary.main" : "#ccc",
            borderRadius: 2,
            p: 4,
            textAlign: "center",
            cursor: "pointer",
            bgcolor: isDragOver ? "action.hover" : "#f9f9f9",
            transition: "all 0.3s ease-in-out",
          }}
          onClick={() => document.getElementById("file-upload-input")?.click()}
        >
          <CloudUploadIcon color="primary" sx={{ fontSize: 40 }} />
          <Typography>拖曳 JSON 檔案到此處，或點擊上傳</Typography>
          <input
            id="file-upload-input"
            type="file"
            multiple={false}
            hidden
            onChange={handleFileChange}
            accept="application/json"
          />
        </Box>
        {file && (
          <Box
            sx={{
              mt: 2,
              display: "flex",
              alignItems: "center",
              justifyContent: "space-between",
              p: 1,
              border: "1px solid #eee",
              borderRadius: "4px",
            }}
          >
            <Typography variant="body2">{file.name}</Typography>
            <IconButton size="small" onClick={handleRemoveFile}>
              <CloseIcon fontSize="small" />
            </IconButton>
          </Box>
        )}
      </DialogContent>
      <DialogActions>
        <Button onClick={handleCancel}>取消</Button>
        <Button
          onClick={handleUpload}
          variant="contained"
          disabled={!file}
        >
          確認上傳並編輯
        </Button>
      </DialogActions>
    </Dialog>
  );
};
```

-----

### 4\. 儀表板頁面導航架構建議

為了完整支援您的需求，您需要在路由中新增兩個頁面：

| 頁面檔案                  | 用途                                      | 導航路徑               | 觸發方式                            |
| :------------------------ | :---------------------------------------- | :--------------------- | :---------------------------------- |
| `DashboardEditorPage.tsx` | 儀表板**編輯**頁面 (新增/修改/上傳後編輯) | `/dashboards/edit`     | 1. 點擊「新增」按鈕                 |
|                           |                                           |                        | 2. 點擊「上傳」後確認               |
|                           |                                           |                        | 3. 在 **瀏覽頁面** 點擊「編輯」按鈕 |
| `DashboardViewPage.tsx`   | 儀表板**瀏覽**頁面 (展示/互動)            | `/dashboards/view/:id` | 1. 點擊列表中的儀表板名稱           |

#### 儀表板編輯頁面 (`DashboardEditorPage.tsx`) 簡要架構

這個頁面將負責處理新增、編輯、和從上傳檔案解析後的編輯邏輯。

```tsx
// src/pages/DashboardEditorPage.tsx (架構)
import { PageWrapper } from "../components/layout/PageWrapper";
import { useParams, useLocation } from "react-router-dom";
import type { UploadDashboardNavigateState, CreateDashboardNavigateState } from "../types";

export const DashboardEditorPage = () => {
  const location = useLocation();
  // 取得導航狀態，判斷是 'create' (點擊新增) 還是 'upload' (上傳後編輯)
  const state = location.state as (UploadDashboardNavigateState | CreateDashboardNavigateState) | undefined;
  
  // 根據 state 決定初始內容
  let initialContent = "從列表點擊編輯或上傳檔案後編輯";
  if (state?.editorMode === 'create') {
      initialContent = "新的儀表板 (建立模式)";
  } else if (state?.editorMode === 'upload' && state.file) {
      initialContent = `從檔案上傳的儀表板: ${state.file.name}`;
      // 實際邏輯：讀取 state.file 內容並解析為儀表板結構
  }

  // ... 編輯邏輯 (圖表拖拉、設定等)

  return (
    <PageWrapper 
      breadcrumbItems={[{ label: "儀表板管理", path: "/dashboards" }, { label: "編輯儀表板", path: "" }]}
      content={(
        <div style={{ padding: 20 }}>
          <h2>儀表板編輯模式</h2>
          <p>內容來源: {initialContent}</p>
          {/* 統計圖表編輯區塊 */}
          <div style={{ height: 500, border: '1px dashed #ccc', marginTop: 20 }}>
            <p style={{ textAlign: 'center', lineHeight: '500px' }}>圖表編輯區塊 (拖拉、新增、刪除)</p>
          </div>
        </div>
      )}
    />
  );
};
```

#### 儀表板瀏覽頁面 (`DashboardViewPage.tsx`) 簡要架構

這個頁面用於展示儀表板，並提供一個「編輯」按鈕。

```tsx
// src/pages/DashboardViewPage.tsx (架構)
import { PageWrapper } from "../components/layout/PageWrapper";
import { useParams, useNavigate } from "react-router-dom";
import { Button } from "@mui/material";

export const DashboardViewPage = () => {
  const { id } = useParams<{ id: string }>(); // 取得 URL 中的儀表板 ID
  const navigate = useNavigate();

  const handleEditClick = () => {
    // 導航到編輯頁面，並帶入 tableId
    navigate("/dashboards/edit", { state: { editorMode: "edit", tableId: id } });
  };

  return (
    <PageWrapper
      breadcrumbItems={[{ label: "儀表板管理", path: "/dashboards" }, { label: `儀表板 #${id} 瀏覽`, path: "" }]}
      content={(
        <div style={{ padding: 20 }}>
          <h2>儀表板 #{id} 瀏覽模式</h2>
          <Button 
            variant="contained" 
            onClick={handleEditClick} 
            style={{ position: 'absolute', top: 15, right: 30 }}
          >
            編輯
          </Button>
          {/* 統計圖表展示區塊 (可互動) */}
          <div style={{ height: 800, border: '1px solid #ccc', marginTop: 20 }}>
            <p style={{ textAlign: 'center', lineHeight: '800px' }}>圖表展示區塊 (可互動)</p>
          </div>
        </div>
      )}
    />
  );
};
```

## Gemini 提問 上傳對話框與上傳狀態管理抽象化
前面提供的上傳對話框以及附件的上傳 uploadStore.ts 都是針對 dataTable 資料表，能否提高泛用性，因為我還希望有能針對 儀表板設定檔、圖表設定檔 的上傳對話框和儲存管理。

> 附件 `uploadStore.ts`

## Gemini 回答 上傳對話框與上傳狀態管理抽象化
您提出的問題非常專業且關鍵。為了讓 **資料表格 (`DataTable`)**、**儀表板 (`Dashboard`)** 和未來的 **圖表 (`Chart`)** 都能使用統一的上傳邏輯和狀態管理，我們需要對現有的 **`uploadStore.ts`** 和 **`UploadDataTableDialog.tsx`** 進行**泛型化**重構。

核心思路是：將 **上傳狀態管理 (Zustand Store)** 和 **通用 UI/UX (Drag & Drop)** 抽象出來，而將 **資源特有邏輯 (API 呼叫、檔案類型、衝突檢查)** 透過 **函式參數** 傳入。

以下是重構的步驟和程式碼：

-----

### 1\. 泛型化上傳狀態管理 (`uploadStore.ts`)

我們將修改 `useUploadStore`，使其不再耦合於 `DataTableInfo` 或 `window.api.uploadTable`。

#### `src/stores/uploadStore.ts` (Refactored)

主要變動：

1.  將 `info` 欄位類型從 `DataTableInfo` 變更為 `ResourceInfo` (`unknown`)。
2.  `UploadInputStatus` 中的 `tableName` 變更為更通用的 `resourceName`。
3.  新增 `UploadHandler` 類型，並將其實作作為參數傳入 `startUploads` 函式。

<!-- end list -->

```typescript
// src/stores/uploadStore.ts
import { create } from "zustand";
import { devtools } from "zustand/middleware";
import { parseDataFile } from "../utils"; // 仍用於數據檔案解析
import type { FileConflictAction } from "../types";
import type { UploadMode } from "shared/types/index";

// 假設所有資源的 Info 類型都不同，我們使用一個泛型類型或 unknown
// 成功上傳後後端返回的資訊類型
type ResourceInfo = unknown; 

// ----------------------------------------------------
// 1. 通用化 Store 中每個上傳任務的狀態介面
// ----------------------------------------------------
export interface FileUploadStatus {
  id: string; 
  fileName: string; 
  status: "uploading" | "success" | "failed";
  progress?: number;
  error?: string;
  errorName?: string;
  info?: ResourceInfo; // 通用化
  timeoutId?: ReturnType<typeof setTimeout>;
  isExpanded?: boolean;
}

// ----------------------------------------------------
// 2. 通用化從對話框傳入的檔案狀態介面
// ----------------------------------------------------
export interface UploadInputStatus {
  id: string;
  resourceName: string; // 從 tableName 通用化
  file: File;
  isConflict: boolean;
  conflictAction: FileConflictAction;
}

// 3. 定義一個通用的上傳處理函式簽名
// 這個函式將在 Store 內部被呼叫，處理資源特有的解析和 API 呼叫邏輯
export type UploadHandler = (
  file: File,
  resourceName: string,
  uploadMode: UploadMode
) => Promise<ResourceInfo>;

// ----------------------------------------------------
// 4. 更新 Store State 介面
// ----------------------------------------------------
interface UploadState {
  uploads: FileUploadStatus[];
  addUpload: (file: File) => string;
  updateUploadStatus: (
    id: string,
    status: FileUploadStatus["status"],
    info?: ResourceInfo, // 通用化
    error?: string,
    errorName?: string
  ) => void;
  // 接受 UploadHandler 函式，由呼叫者提供
  startUploads: (
    filesStatus: UploadInputStatus[],
    uploadHandler: UploadHandler
  ) => Promise<void>;
  reset: () => void;
  removeUpload: (id: string) => void;
  toggleExpand: (id: string) => void;
}

export const useUploadStore = create<UploadState>()(
  devtools(
    (set, get) => ({
      // ... (其他 action 保持不變：addUpload, updateUploadStatus, removeUpload, toggleExpand, reset)

      // 5. 重構 startUploads 邏輯：使用傳入的 uploadHandler
      startUploads: async (
        filesStatus: UploadInputStatus[],
        uploadHandler: UploadHandler // 接收傳入的處理函式
      ) => {
        const { updateUploadStatus, addUpload, removeUpload } = get();

        const uploadPromises = filesStatus
          .filter((fileStatus) => fileStatus.conflictAction !== "skip")
          .map(async (fileStatus) => {
            const newId = addUpload(fileStatus.file); 
            const finalResourceName = fileStatus.resourceName;
            const uploadMode: UploadMode =
              fileStatus.isConflict && fileStatus.conflictAction === "replace"
                ? "replace"
                : "create"; 

            try {
              // 呼叫傳入的通用處理函式來執行上傳
              const resourceInfo = await uploadHandler(
                fileStatus.file,
                finalResourceName,
                uploadMode
              );

              updateUploadStatus(newId, "success", resourceInfo);

              setTimeout(() => {
                removeUpload(newId);
              }, 10000);
            } catch (e: unknown) {
              // 處理錯誤
              const errorMsg = e instanceof Error ? e.message : "未知錯誤";
              const errorName = e instanceof Error ? e.name : "Error";

              updateUploadStatus(newId, "failed", undefined, errorMsg, errorName);

              setTimeout(() => {
                removeUpload(newId);
              }, 30000);
            }
          });

        await Promise.allSettled(uploadPromises);
      },
    }),
    { name: "upload-store" }
  )
);
```

-----

### 2\. 建立通用上傳對話框 (`GenericUploadDialog.tsx`)

將 `UploadDataTableDialog.tsx` 中的 UI 元素、拖曳邏輯和衝突檢查邏輯抽象化，以便所有資源類型都能重複使用。

#### `src/components/common/GenericUploadDialog.tsx` (新檔案)

```typescript
// src/components/common/GenericUploadDialog.tsx
import { useEffect, useState, useMemo } from "react";
import {
  Dialog, DialogTitle, DialogContent, DialogActions, Button,
  Typography, Box, IconButton, FormControl, Select, MenuItem,
} from "@mui/material";
import CloudUploadIcon from "@mui/icons-material/CloudUpload";
import WarningIcon from "@mui/icons-material/Error";
import CheckCircleIcon from "@mui/icons-material/CheckCircle";
import CloseIcon from "@mui/icons-material/Close";
import { getNameFromFile } from "../../utils";
import { useDebounce } from "../../hooks/useDebounce";
import type { FileConflictAction } from "../../types";
import type { ConflictResult } from "shared/types/index";

// ----------------------------------------------------
// 通用檔案狀態介面 (供外部使用)
// ----------------------------------------------------
export interface GenericFileStatus {
  id: string;
  resourceName: string;
  file: File;
  isConflict: boolean;
  conflictAction: FileConflictAction;
}

// ----------------------------------------------------
// 通用 Props 介面
// ----------------------------------------------------
interface Props<T extends string = string> {
  open: boolean;
  onClose: () => void;
  title: string;
  // 資源類型
  resourceType: 'table' | 'dashboard' | 'chart';
  // 可接受的檔案類型 (e.g., "text/csv, application/json")
  fileAccept: string; 
  // 單檔案/多檔案上傳模式
  isMultiFileUpload: boolean; 
  // 檢查衝突的 API 呼叫函式 (由各資源元件提供)
  checkConflict: (names: string[]) => Promise<ConflictResult[]>;
  // 處理單一檔案上傳完成並導航的邏輯 (例如：跳轉到編輯頁面)
  onSingleFileConfirmed: (file: File) => void;
  // 處理多檔案上傳 (例如：呼叫 Store 的 startUploads) 的邏輯
  onMultiFilesConfirmed: (filesStatus: GenericFileStatus[]) => void;
  // (Optional) 針對特定資源的額外選項 UI (例如 DataTable 的 Mode 1/2)
  extraOptions?: {
    ui: React.ReactNode;
    fileFilter?: (file: File, selectedMode: T) => boolean; 
    selectedMode: T;
  };
}

export const GenericUploadDialog = <T extends string = string>({
  open, onClose, title, resourceType, fileAccept, isMultiFileUpload, 
  checkConflict, onSingleFileConfirmed, onMultiFilesConfirmed, extraOptions
}: Props<T>) => {
  const [filesStatus, setFilesStatus] = useState<GenericFileStatus[]>([]);
  const [isDragOver, setIsDragOver] = useState(false);
  const debouncedFilesStatus = useDebounce(filesStatus, 300);

  const existingFileIds = useMemo(
    () => new Set(filesStatus.map((f) => f.id)),
    [filesStatus]
  );

  const preprocessFiles = (files: FileList | null): GenericFileStatus[] => {
    if (!files) return [];
    
    const fileArray = Array.from(files);
    
    // 檔案類型過濾
    const filterFn = extraOptions?.fileFilter 
      ? (file: File) => extraOptions.fileFilter!(file, extraOptions.selectedMode) 
      : (file: File) => fileAccept.includes(file.type);

    return fileArray
      .filter(filterFn)
      .filter((file) => !existingFileIds.has(file.name))
      .slice(0, isMultiFileUpload ? undefined : 1)
      .map((file) => ({
        id: file.name,
        resourceName: getNameFromFile(file.name),
        file: file,
        isConflict: false,
        conflictAction: "rename" as FileConflictAction,
      }));
  };

  const handleCancel = () => {
    setFilesStatus([]);
    onClose();
  };

  const handleFileChange = (event: React.ChangeEvent<HTMLInputElement>) => {
    if (event.target.files) {
      setFilesStatus((prev) => [
        ...(isMultiFileUpload ? prev : []),
        ...preprocessFiles(event.target.files),
      ]);
      event.target.value = ''; 
    }
  };
  
  // (拖曳處理器邏輯與 UploadDataTableDialog.tsx 中相同)

  const handleDrop = (event: React.DragEvent<HTMLDivElement>) => {
      event.preventDefault();
      setIsDragOver(false);
      if (event.dataTransfer?.files) {
        setFilesStatus((prev) => [
          ...(isMultiFileUpload ? prev : []),
          ...preprocessFiles(event.dataTransfer.files),
        ]);
      }
  };

  const handleRemoveFile = (fileId: string) => {
    setFilesStatus(filesStatus.filter((status) => status.id !== fileId));
  };

  const handleActionChange = (
    fileId: string,
    conflictAction: GenericFileStatus["conflictAction"]
  ) => {
    setFilesStatus(
      filesStatus.map((fileStatus) =>
        fileStatus.id === fileId
          ? { ...fileStatus, conflictAction }
          : fileStatus
      )
    );
  };

  const handleUpload = () => {
    if (filesStatus.length === 0) return;

    onClose();
    if (filesStatus.length === 1 && !isMultiFileUpload) {
      onSingleFileConfirmed(filesStatus[0].file);
    } else {
      onMultiFilesConfirmed(filesStatus);
    }
    setFilesStatus([]);
  };

  // --- 衝突檢查邏輯 ---
  useEffect(() => {
    if (debouncedFilesStatus.length > 0) {
      const resourceNamesToCheck = debouncedFilesStatus.map((f) => f.resourceName);

      checkConflict(resourceNamesToCheck)
        .then((conflictResults: ConflictResult[]) => {
          if (conflictResults.length !== debouncedFilesStatus.length) {
            console.error("Conflict check result length mismatch!");
            return;
          }
          // 根據衝突結果更新狀態
          setFilesStatus((prevStatus) => 
            prevStatus.map((status, i) => {
              const conflictResult = conflictResults[i];
              return { ...status, isConflict: conflictResult.isConflict };
            })
          );
        })
        .catch((error) => {
          console.error(`Error checking ${resourceType} conflicts:`, error);
        });
    }
  }, [debouncedFilesStatus, resourceType, checkConflict]);

  const isConfirmDisabled = filesStatus.length === 0;

  return (
    <Dialog open={open} onClose={handleCancel} maxWidth="sm" fullWidth>
      <DialogTitle>{title}</DialogTitle>
      <IconButton
        aria-label="close"
        onClick={handleCancel}
        sx={(theme) => ({
          position: "absolute",
          right: 8, top: 8, color: theme.palette.grey[500],
        })}
      >
        <CloseIcon />
      </IconButton>
      <DialogContent dividers>
        {/* 額外選項 UI (例如 DataTable 的 Mode 1/2) */}
        {extraOptions && (
          <Box mb={2}>
            {extraOptions.ui}
          </Box>
        )}
        
        <Typography variant="subtitle1" sx={{ mt: extraOptions ? 0 : 2 }} gutterBottom>
          上傳檔案
        </Typography>
        <Box
          onDragOver={(e) => { e.preventDefault(); setIsDragOver(true); }}
          onDragLeave={(e) => { e.preventDefault(); setIsDragOver(false); }}
          onDrop={handleDrop}
          // ... (Drag & Drop 樣式和 input 元素)
          sx={{ /* ... 樣式 ... */ }}
          onClick={() => document.getElementById("file-upload-input")?.click()}
        >
          {/* ... (UI elements) */}
          <CloudUploadIcon color="primary" sx={{ fontSize: 40 }} />
          <Typography>拖曳檔案到此處，或點擊上傳</Typography>
          <input
            id="file-upload-input"
            type="file"
            multiple={isMultiFileUpload}
            hidden
            onChange={handleFileChange}
            accept={fileAccept}
          />
        </Box>
        {/* ... (Files List UI and Conflict Handling) ... */}
        {filesStatus.length > 0 && (
          <Box sx={{ mt: 2 }}>
            <Typography variant="body2" sx={{ mb: 1 }}>
              已選擇檔案:
            </Typography>
            <ul style={{ listStyleType: "none", padding: 0 }}>
              {filesStatus.map((fileStatus) => (
                <li
                  key={fileStatus.id}
                  style={{ /* ... 樣式 ... */ }}
                >
                  <Typography variant="body2" sx={{ flexGrow: 1, mr: 2 }}>
                    {fileStatus.id}
                  </Typography>
                  {fileStatus.isConflict ? (
                    <Box sx={{ display: "flex", alignItems: "center", flexShrink: 0 }}>
                      <WarningIcon color="warning" sx={{ mr: 1 }} />
                      <FormControl variant="standard" size="small">
                        <Select
                          value={fileStatus.conflictAction}
                          onChange={(e) =>
                            handleActionChange(fileStatus.id, e.target.value as FileConflictAction)
                          }
                        >
                          <MenuItem value="rename">自動更名 (新檔案)</MenuItem>
                          <MenuItem value="replace">更新替換 (覆蓋舊資料)</MenuItem>
                          <MenuItem value="skip">忽略 (不處理)</MenuItem>
                        </Select>
                      </FormControl>
                    </Box>
                  ) : (
                    <Box sx={{ display: "flex", alignItems: "center", flexShrink: 0 }}>
                      <CheckCircleIcon color="success" sx={{ mr: 1 }} />
                      <Typography variant="caption">無衝突</Typography>
                    </Box>
                  )}
                  <IconButton
                    size="small"
                    onClick={() => handleRemoveFile(fileStatus.id)}
                    sx={{ p: 0.5, ml: 1 }}
                  >
                    <CloseIcon fontSize="small" />
                  </IconButton>
                </li>
              ))}
            </ul>
          </Box>
        )}
      </DialogContent>
      <DialogActions>
        <Button onClick={handleCancel}>取消</Button>
        <Button
          onClick={handleUpload}
          variant="contained"
          disabled={isConfirmDisabled}
        >
          {filesStatus.length === 1 && !isMultiFileUpload ? "確認上傳並編輯" : "確認"}
        </Button>
      </DialogActions>
    </Dialog>
  );
};
```

-----

### 3\. 實作資源特定的上傳對話框 (Wrapper)

#### 3.1. 資料表格上傳 (`UploadDataTableDialog.tsx`)

現在它作為一個 **Wrapper**，將 `DataTable` 專屬的邏輯（如 `Mode 1/2` 選項和 `uploadTable` API 呼叫）提供給通用元件。

```typescript
// src/components/DataTablesPage/UploadDataTableDialog.tsx
import { useState, useMemo } from "react";
import { RadioGroup, Radio, FormControlLabel, Typography } from "@mui/material";
import { useNavigate } from "react-router-dom";
// 引入通用元件和通用 Store 類型
import { GenericUploadDialog, GenericFileStatus } from "../common/GenericUploadDialog";
import { useUploadStore, UploadHandler } from "../../stores/uploadStore";
// 引入必要的工具函式和類型
import { parseDataFile } from "../../utils";
import type { UploadTableNavigateState } from "../../types";
import type { DataTableInfo } from "shared/types/dataTable"; 

interface Props {
  open: boolean;
  onClose: () => void;
}

// 實作 Table 專屬的上傳處理函式 (傳入 Store)
const handleTableUpload: UploadHandler = async (file, resourceName, uploadMode) => {
  const parsedData = await parseDataFile(file); // 1. 解析檔案數據
  const tableInfo = { name: resourceName, description: "" };
  
  // 2. 呼叫後端 API 執行上傳
  const dataTableInfo = await window.api.uploadTable(
    tableInfo,
    parsedData,
    uploadMode
  );
  
  return dataTableInfo as DataTableInfo; // 類型斷言
};

export const UploadDataTableDialog = ({ open, onClose }: Props) => {
  const [uploadMode, setUploadMode] = useState<"mode1" | "mode2">("mode1");
  const navigate = useNavigate();
  const { startUploads } = useUploadStore();

  // 1. 處理單一檔案確認：導航到編輯頁面
  const handleSingleFileConfirmed = (file: File) => {
    onClose();
    const state: UploadTableNavigateState = { editorMode: "upload", file: file };
    navigate("/data-tables/edit", { state });
  };

  // 2. 處理多檔案確認：呼叫 Store 的 action
  const handleMultiFilesConfirmed = (filesStatus: GenericFileStatus[]) => {
    startUploads(filesStatus, handleTableUpload);
  };
  
  // 3. Table 專屬的衝突檢查 API 呼叫
  const checkConflictApi = (tableNames: string[]) => {
    return window.api.checkTablesConflict(tableNames);
  };

  // 4. Table 專屬的 Mode 選擇 UI 和過濾邏輯
  const extraOptions = useMemo(() => ({
    ui: (
      <>
        <Typography variant="subtitle1" gutterBottom>上傳模式</Typography>
        <RadioGroup
          row
          value={uploadMode}
          onChange={(e) => setUploadMode(e.target.value as "mode1" | "mode2")}
        >
          <FormControlLabel value="mode1" control={<Radio />} label="模式一：僅上傳資料內容" />
          <FormControlLabel value="mode2" control={<Radio />} label="模式二：包含資訊與資料" />
        </RadioGroup>
      </>
    ),
    fileFilter: (file: File, selectedMode: "mode1" | "mode2") =>
      selectedMode === "mode1"
        ? file.type === "text/csv" || file.type === "application/json"
        : file.type === "application/json",
    selectedMode: uploadMode,
  }), [uploadMode]);

  return (
    <GenericUploadDialog
      open={open}
      onClose={onClose}
      title="上傳資料表格"
      resourceType="table"
      fileAccept="text/csv, application/json"
      isMultiFileUpload={true} // DataTable 支援多檔案上傳
      checkConflict={checkConflictApi}
      onSingleFileConfirmed={handleSingleFileConfirmed}
      onMultiFilesConfirmed={handleMultiFilesConfirmed}
      extraOptions={extraOptions}
    />
  );
};
```

#### 3.2. 儀表板上傳 (`UploadDashboardDialog.tsx`)

新建立的儀表板上傳對話框，只需要提供 `Dashboard` 專屬的邏輯。

```typescript
// src/components/DashboardsPage/UploadDashboardDialog.tsx
import { useNavigate } from "react-router-dom";
import { GenericUploadDialog, GenericFileStatus } from "../common/GenericUploadDialog";
import { useUploadStore, UploadHandler } from "../../stores/uploadStore";
import type { UploadDashboardNavigateState } from "../../types";
import type { DashboardInfo } from "shared/types/dashboard";

interface Props {
  open: boolean;
  onClose: () => void;
}

// 實作 Dashboard 專屬的上傳處理函式 (傳入 Store)
const handleDashboardUpload: UploadHandler = async (file, resourceName, uploadMode) => {
  const fileContent = await file.text(); // 讀取檔案內容
  
  const dashboardConfig = {
    name: resourceName,
    content: JSON.parse(fileContent),
  };
  
  // 假設後端有一個 window.api.uploadDashboard 方法
  const dashboardInfo = await window.api.uploadDashboard(
    dashboardConfig,
    uploadMode
  );
  
  return dashboardInfo as DashboardInfo;
};


export const UploadDashboardDialog = ({ open, onClose }: Props) => {
  const navigate = useNavigate();
  const { startUploads } = useUploadStore();

  // 1. 處理單一檔案確認：導航到編輯頁面
  const handleSingleFileConfirmed = (file: File) => {
    onClose();
    const state: UploadDashboardNavigateState = { editorMode: "upload", file: file };
    navigate("/dashboards/edit", { state });
  };
  
  // 2. 處理多檔案確認：(Dashboard 通常是單檔案，此處遵循通用 Store 結構)
  const handleMultiFilesConfirmed = (filesStatus: GenericFileStatus[]) => {
    // 雖然儀表板通常是單一檔案，但如果多檔案上傳，則呼叫 startUploads
    startUploads(filesStatus, handleDashboardUpload);
  };
  
  // 3. Dashboard 專屬的衝突檢查 API 呼叫
  const checkConflictApi = (dashboardNames: string[]) => {
    // 假設後端有一個 window.api.checkDashboardsConflict 方法
    return window.api.checkDashboardsConflict(dashboardNames); 
  };
  
  return (
    <GenericUploadDialog
      open={open}
      onClose={onClose}
      title="上傳儀表板設定檔"
      resourceType="dashboard"
      fileAccept="application/json" 
      isMultiFileUpload={false} // 儀表板通常是單一檔案
      checkConflict={checkConflictApi}
      onSingleFileConfirmed={handleSingleFileConfirmed}
      onMultiFilesConfirmed={handleMultiFilesConfirmed}
      // Dashboard 沒有額外選項
    />
  );
};
```

#### 3.3. 更新儀表板管理頁面 (`DashboardsPage.tsx`)

最後，將 `DashboardsPage.tsx` 更新為使用新的 `UploadDashboardDialog`，並啟用新增/上傳按鈕功能。

```typescript
// src/pages/DashboardsPage.tsx
// ... (imports)
import { useNavigate } from "react-router-dom"; 
import { UploadDashboardDialog } from "../components/DashboardsPage/UploadDashboardDialog"; // 使用新建立的 Dashboard Dialog
// ... (rest of the file)

export const DashboardsPage = () => {
  const [searchText, setSearchText] = useState("");
  const [dashboardInfos, setDashboardInfos] = useState<DashboardInfo[]>([]); 
  const [uploadDialogOpen, setUploadDialogOpen] = useState(false); 
  const navigate = useNavigate(); 

  // ... (refreshDashboardInfos 函數)

  const handleNewDashboardClick = () => {
    const state = { editorMode: "create" }; 
    navigate("/dashboards/edit", { state });
  };

  // ... (filteredDashboards 邏輯)

  const pageConfig: Omit<PageConfig, "tocItems"> = {
    breadcrumbItems: [{ label: "儀表板管理", path: "/dashboards" }],
    content: (
      <>
        <GenericListPage
          title="儀表板管理"
          items={filteredDashboards}
          searchable
          creatable // 開啟新增按鈕
          uploadable // 開啟上傳按鈕
          searchPlaceholder="搜尋儀表板"
          onSearch={setSearchText}
          onCreate={handleNewDashboardClick} // 實作新增處理
          onUpload={() => setUploadDialogOpen(true)} // 實作上傳處理
          renderList={(items, viewMode) => (
            <DashboardList
              dashboards={items}
              viewMode={viewMode}
              refresh={refreshDashboardInfos}
            />
          )}
        />
        {/* 上傳儀表板對話框 */}
        <UploadDashboardDialog
          open={uploadDialogOpen}
          onClose={() => setUploadDialogOpen(false)}
        />
      </>
    ),
    rightPanelContent: null,
    rightPanelTitle: "上傳狀態",
  };

  return <PageWrapper {...pageConfig} />;
};
```

透過上述重構，您現在擁有了高度可重用的上傳元件和狀態管理，未來新增圖表上傳時，只需建立一個 `UploadChartDialog.tsx` Wrapper 即可。

## Gemini 提問 
## Gemini 回答 
## Gemini 提問 
## Gemini 回答 
## Gemini 提問 
## Gemini 回答 